# NextAuth Configuration
# ============================================================================
# CRITICAL: NEXTAUTH_SECRET is REQUIRED for production deployments
# This variable is validated at build time and the build will fail if it's missing
# ============================================================================
# Generate a secret using: openssl rand -base64 32
# This secret is used to sign and encrypt JWT tokens and must be set in production
# For local development, you can use any non-empty string, but a secure random value is recommended
NEXTAUTH_SECRET=your_secret_key_here

# NextAuth URL (used for OAuth callbacks)
# This variable is OPTIONAL and will be automatically determined based on the environment:
# - Local Development: MUST use http:// (not https://) for localhost
#   e.g. NEXTAUTH_URL=http://localhost:3000
# - Production (non-Vercel): Set this to your production domain — MUST use https://
#   e.g. NEXTAUTH_URL=https://yourdomain.com
# - Vercel Production: Leave unset — will be automatically set from VERCEL_PROJECT_PRODUCTION_URL
# - Vercel Preview: Leave unset — will be automatically set from VERCEL_URL
#
# ⚠️  PROTOCOL MATTERS:
#   - Use http://  for local development  (http://localhost:3000)
#   - Use https:// for all production/staging deployments
#
# For Vercel deployments, NEXTAUTH_URL is automatically bootstrapped at runtime from:
# 1. VERCEL_PROJECT_PRODUCTION_URL (for production deployments)
# 2. VERCEL_URL (for preview deployments)
# So you do NOT need to set this as a GitHub Secret for Vercel deployments.
NEXTAUTH_URL=http://localhost:3000

# Yahoo OAuth Callback URL
# ============================================================================
# IMPORTANT: DO NOT SET THIS VARIABLE MANUALLY.
#
# YAHOO_CALLBACK_URL is automatically generated at runtime by lib/get-callback-url.ts.
# It is derived from NEXTAUTH_URL (or VERCEL_PROJECT_PRODUCTION_URL or VERCEL_URL) by appending /api/auth/callback/yahoo.
#
# The callback URL is dynamically computed based on your environment:
#   - Local Dev:    NEXTAUTH_URL=http://localhost:3000   => http://localhost:3000/api/auth/callback/yahoo
#   - Production:   NEXTAUTH_URL=https://yourdomain.com  => https://yourdomain.com/api/auth/callback/yahoo
#   - Vercel Prod:  VERCEL_PROJECT_PRODUCTION_URL=my-app.vercel.app => https://my-app.vercel.app/api/auth/callback/yahoo
#   - Vercel Prev:  VERCEL_URL=my-app-abc123.vercel.app  => https://my-app-abc123.vercel.app/api/auth/callback/yahoo
#
# To register your callback URL in Yahoo's Developer Console:
# 1. Go to https://developer.yahoo.com/apps
# 2. Select your application
# 3. In "Redirect URIs", add the appropriate callback URL for your environment
# 4. For local development: http://localhost:3000/api/auth/callback/yahoo
#    (must match the http:// protocol — Yahoo will reject https://localhost)
# 5. For production: https://yourdomain.com/api/auth/callback/yahoo
#
# See docs/YAHOO_OAUTH_SETUP.md for detailed setup instructions.
# ============================================================================

# Yahoo OAuth Provider
# Create an OAuth app at: https://developer.yahoo.com/apps
# For local development, set Redirect URIs to: http://localhost:3000/api/auth/callback/yahoo
# For production, set Redirect URIs to: https://yourdomain.com/api/auth/callback/yahoo
YAHOO_AUTH_URL=https://api.login.yahoo.com/oauth2/request_auth
YAHOO_TOKEN_URL=https://api.login.yahoo.com/oauth2/get_token
YAHOO_CLIENT_ID=your_yahoo_client_id
YAHOO_CLIENT_SECRET=your_yahoo_client_secret

# League Standings
# ============================================================================
# Standings are fetched directly from the Yahoo Fantasy /standings endpoint
# via GET /api/leagueinfo/[id]. No additional configuration is required.
#
# The previous NEXT_PUBLIC_SEASON_START_WEEK and NEXT_PUBLIC_SEASON_END_WEEK
# variables (used for multi-week stat aggregation) are no longer needed and
# should be removed from your environment if present.
# ============================================================================
